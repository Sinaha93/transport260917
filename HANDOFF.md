# 상차·출하 정보 공유 프로그램 인수인계서

> 최종 갱신: 2026-09-17
>
> 저장소: <https://github.com/Sinaha93/transport260917>
>
> 기준 브랜치: `main`
>
> 1단계 완료 커밋: `26a462a`
>
> 현재 상태: **1단계 완료, 2단계 미착수**

## 1. 이 문서의 목적

이 문서는 다른 컴퓨터에서 Codex 또는 개발자가 현재 구현 상태를 정확히 파악하고 다음 단계부터 이어서 작업하기 위한 기준 문서입니다.

이 프로젝트는 단계별로 구현합니다. 한 단계를 구현하고 테스트·커밋·GitHub 푸시까지 마치면 반드시 작업을 멈춥니다. 사용자가 다음 단계 시작을 지시하기 전에는 후속 단계를 함께 구현하지 않습니다.

## 2. 프로젝트 목표

자동차 부품 물류팀이 ERP/PDA 출하 기록을 바탕으로 다음 업무를 수행하는 사무실용 내부 웹 프로그램을 구축합니다.

- 차량별 상차 계획 수립
- 철제 파렛트 단위 적재율 계산 및 제약 검증
- 자동 배치안 생성과 3D 적재 상태 확인
- 상차 계획 확정·공유 및 변경 이력 관리
- 월별 출하계획 대비 실제 출하실적 분석

현장 작업자는 새 프로그램을 사용하지 않습니다. 기존 PDA 출하 절차는 그대로 유지하며, 새 프로그램은 ERP/PDA 데이터를 읽어 사무실에서 계획·검토·분석하는 용도입니다.

## 3. 확정된 업무 전제

- 운영 차량은 `5t`, `5t축`, `11t`이며 실제 적재함 제원은 기준정보로 등록합니다.
- 차량별 담당 납품처는 대체로 정해져 있습니다.
- 운송 경로는 운전기사가 결정하므로 경로 최적화는 이 프로젝트의 핵심 범위가 아닙니다.
- 출하는 여러 규격의 철제 파렛트 단위로 이루어집니다.
- 보통 2단 적재하지만 적층할 수 없는 품목도 있습니다.
- 품목별 회전 허용 여부, 파렛트당 수량, 중량 등의 제약정보가 필요합니다.
- ERP/PDA에는 품목과 수량 중심의 출하실적이 존재합니다.
- ERP에 없는 파렛트 규격·적층·회전 정보는 새 프로그램에서 관리합니다.
- ERP 데이터는 변경하지 않고 읽기만 하는 것을 원칙으로 합니다.
- ERP 연결 방식은 아직 확정되지 않았습니다. API, 읽기 전용 DB, CSV 중 실제 가능한 방법을 선택합니다.
- ERP 비밀번호, DB 접속정보, API 키 등 비밀정보는 Git에 저장하지 않습니다.

## 4. 전체 구현 단계

| 단계 | 범위 | 상태 | 단계 종료 조건 |
|---|---|---|---|
| 1 | 기본 웹앱, 차량·파렛트·품목 기준정보 | 완료 | 등록·조회·삭제, DB 저장, 테스트·빌드·푸시 |
| 2 | ERP/PDA 출하정보 연동 기반 | 미착수 | 교체 가능한 어댑터, CSV 우선 수신, 동기화 이력·오류·재시도 |
| 3 | 파렛트 환산, 적재율, 제약 검증 | 미착수 | 출하수량 환산, 중량·면적·부피 적재율, 위반 검출 |
| 4 | 자동 배치와 3D 상차 검토 | 미착수 | 규칙 기반 자동배치, 3D 이동·회전·적층, 확정·버전관리 |
| 5 | 월별 출하계획 대비 실적 분석 | 미착수 | 달성률·누적선·예상 달성률·계획 변경 이력 |
| 6 | 통합 안정화와 운영 문서 | 미착수 | 권한·감사·백업·오류 대응·최종 사용자 문서 |

### 단계 진행 원칙

1. 한 번에 한 단계만 구현합니다.
2. 각 단계에서 테스트를 먼저 추가하거나 수정합니다.
3. `test`, `lint`, 타입 검사, 프로덕션 빌드를 모두 통과시킵니다.
4. 주요 화면과 API를 실제로 실행해 확인합니다.
5. 해당 단계만 Git 커밋하고 `origin/main`에 푸시합니다.
6. 완료 결과를 보고하고 다음 단계는 시작하지 않습니다.

## 5. 1단계 구현 완료 내용

### 화면

- 기준정보 전체 현황을 보여주는 한국어 대시보드
- 차량, 철제 파렛트, 품목 탭
- 등록·목록 조회·삭제 기능
- 로딩, 빈 목록, 성공, 오류 상태 표시
- 사무실 PC와 작은 화면을 고려한 반응형 레이아웃

### 차량 기준정보

- 차량명과 차종
- 적재함 내부 길이·폭·높이(mm)
- 최대 적재중량(kg)
- 축하중 제한(kg)
- 도어 위치

### 철제 파렛트 기준정보

- 파렛트명
- 길이·폭·높이(mm)
- 공차중량과 최대 총중량(kg)
- 적층 가능 여부와 최대 적층 단수
- 90도 회전 허용 여부

### 품목 기준정보

- ERP 품목코드와 품목명
- 적용 파렛트 연결
- 파렛트당 적재수량
- 제품 단위중량(kg)

### 저장 및 검증

- Cloudflare D1 호환 SQLite 데이터베이스
- Drizzle ORM 스키마와 최초 마이그레이션
- 서버 API에서 입력값 검증
- 허용되지 않은 필드는 저장하지 않음
- 비적층 파렛트의 최대 적층수를 1로 정규화
- 품목코드 중복 방지

### 1단계 검증 결과

- Node 자동 테스트 5개 통과
- ESLint 통과
- TypeScript 검사 통과
- 프로덕션 빌드 성공
- 메인 화면과 차량·파렛트·품목 API 응답 확인
- 차량 → 파렛트 → 품목 등록 흐름을 브라우저에서 확인

## 6. 기술 구성

- Node.js 22.13 이상
- TypeScript 5
- React 19
- Next.js 호환 Vinext/Vite 실행 환경
- Tailwind CSS 4
- Drizzle ORM
- Cloudflare D1/SQLite
- Wrangler 로컬 실행 환경
- Node 내장 테스트 러너

인증과 사용자 권한은 아직 구현하지 않았습니다. 현재는 내부 개발용 단계입니다.

## 7. 주요 파일 구조

```text
app/
  api/vehicles/route.ts     차량 API
  api/pallets/route.ts      파렛트 API
  api/products/route.ts     품목 API
  master-dashboard.tsx      기준정보 대시보드와 입력 화면
  globals.css               전체 화면 스타일
  layout.tsx                한국어 메타데이터와 공통 레이아웃
db/
  index.ts                  D1 데이터베이스 연결
  schema.ts                 차량·파렛트·품목 테이블 정의
drizzle/
  0000_giant_zuras.sql      최초 DB 마이그레이션
lib/
  master-data.mjs           기준정보 검증과 요약 규칙
tests/
  master-data.test.mjs      도메인 규칙 자동 테스트
.openai/hosting.json        D1 바인딩 설정
README.md                   설치와 기본 실행 방법
HANDOFF.md                  전체 계획과 작업 인수인계 문서
```

## 8. 현재 데이터베이스 구조

### `vehicles`

| 필드 | 의미 |
|---|---|
| `id` | UUID 기본키 |
| `name` | 차량명 |
| `type` | 5t, 5t축, 11t 등의 구분 |
| `length_mm`, `width_mm`, `height_mm` | 적재함 내부 치수 |
| `max_load_kg` | 최대 적재중량 |
| `axle_limit_kg` | 축하중 제한 |
| `door_side` | 도어 위치, 현재 기본값 `REAR` |
| `created_at` | 등록시각 |

### `pallets`

| 필드 | 의미 |
|---|---|
| `id` | UUID 기본키 |
| `name` | 파렛트명 |
| `length_mm`, `width_mm`, `height_mm` | 파렛트 외곽 치수 |
| `tare_weight_kg` | 파렛트 공차중량 |
| `max_gross_weight_kg` | 최대 총중량 |
| `stackable` | 적층 가능 여부 |
| `max_stack` | 최대 적층 단수 |
| `rotatable` | 90도 회전 허용 여부 |
| `created_at` | 등록시각 |

### `products`

| 필드 | 의미 |
|---|---|
| `id` | UUID 기본키 |
| `code` | ERP 품목코드, 고유값 |
| `name` | 품목명 |
| `pallet_id` | 연결된 파렛트 외래키 |
| `units_per_pallet` | 파렛트당 적재수량 |
| `unit_weight_kg` | 제품 단위중량 |
| `created_at` | 등록시각 |

스키마 변경 시 `db/schema.ts`를 먼저 수정하고 아래 명령으로 새 마이그레이션을 생성합니다.

```powershell
npm run db:generate
```

기존 마이그레이션 파일을 수정하지 말고 새 마이그레이션을 추가합니다.

## 9. 현재 API

| 경로 | 메서드 | 기능 |
|---|---|---|
| `/api/vehicles` | `GET` | 차량 목록 |
| `/api/vehicles` | `POST` | 차량 등록 |
| `/api/vehicles?id=...` | `DELETE` | 차량 삭제 |
| `/api/pallets` | `GET` | 파렛트 목록 |
| `/api/pallets` | `POST` | 파렛트 등록 |
| `/api/pallets?id=...` | `DELETE` | 파렛트 삭제 |
| `/api/products` | `GET` | 품목 목록 |
| `/api/products` | `POST` | 품목 등록 |
| `/api/products?id=...` | `DELETE` | 품목 삭제 |

수정 API와 일괄 업로드 API는 아직 없습니다.

## 10. 다른 컴퓨터에서 작업 시작하기

이미 GitHub 저장소를 연결한 컴퓨터라면 PowerShell에서 저장소 폴더로 이동한 뒤 실행합니다.

```powershell
git checkout main
git pull --ff-only origin main
npm ci
npm run build
```

해당 컴퓨터에서 로컬 DB를 처음 만드는 경우에만 다음 마이그레이션 명령을 실행합니다.

```powershell
node --import ./scripts/sites-env.mjs ./node_modules/wrangler/bin/wrangler.js d1 execute DB --local --config dist/server/wrangler.json --persist-to .wrangler/state --file drizzle/0000_giant_zuras.sql
```

그다음 프로그램을 시작합니다.

```powershell
npm start
```

기본 접속 주소는 Wrangler가 시작 로그에 표시하는 주소(일반적으로 `http://127.0.0.1:8787`)입니다. 다른 포트를 사용하려면 다음처럼 실행합니다.

```powershell
npm start -- --port 4174
```

Windows PowerShell 실행 정책 때문에 `npm` 실행이 막히면 같은 명령을 `npm.cmd`로 실행합니다.

```powershell
npm.cmd ci
npm.cmd run build
npm.cmd start
```

### 로컬 데이터에 관한 주의사항

- 로컬 DB는 `.wrangler/state`에 있으며 Git에 포함되지 않습니다.
- 기존 PC에서 시험 등록한 차량·파렛트·품목은 다른 PC로 자동 이동하지 않습니다.
- GitHub에는 프로그램 코드와 DB 구조만 저장됩니다.
- 운영 데이터 이전 기능은 후속 단계에서 별도로 설계해야 합니다.

## 11. 개발 전후 필수 확인 명령

작업 시작 전:

```powershell
git status
git pull --ff-only origin main
```

작업 완료 전:

```powershell
npm test
npm run lint
npx tsc --noEmit
npm run build
git diff --check
git status
```

프로덕션 미리보기가 실행 중이면 Windows에서 `dist` 폴더 잠금 때문에 빌드가 실패할 수 있습니다. 이 경우 실행 중인 `npm start`를 `Ctrl+C`로 종료한 뒤 다시 빌드합니다.

## 12. 다음 작업: 2단계 ERP/PDA 출하정보 연동 기반

다음 구현은 **2단계만** 수행합니다. 3단계의 적재율 계산이나 3D 배치는 함께 구현하지 않습니다.

### 2단계 목표

ERP 연결방식이 바뀌어도 프로그램 내부 기능은 바뀌지 않도록 교체 가능한 수신 어댑터 구조를 만듭니다. 실제 API 또는 DB 접속정보가 아직 없으므로 CSV 가져오기를 먼저 실제 동작하게 만들고, API·읽기 전용 DB 방식은 동일 인터페이스의 골격으로 준비합니다.

### 2단계 권장 데이터 항목

출하 헤더:

- 출하번호
- 출하일
- 납품처 코드와 명칭
- 배정 차량 또는 차량 구분
- PDA 출하 상태
- PDA 출하 완료시각
- ERP 최종 변경시각

출하 상세:

- 출하번호
- ERP 품목코드
- 품목명
- 출하수량
- 수량 단위
- 납품처
- 하차 순서가 있으면 해당 순번

동기화 이력:

- 실행 ID
- 연동 방식(`CSV`, `API`, `READ_ONLY_DB`)
- 시작·종료시각
- 성공·실패 상태
- 전체·성공·실패 건수
- 오류 요약
- 재시도 대상 정보

### 2단계 권장 테이블

- `shipments`: 출하 헤더
- `shipment_lines`: 출하 품목 상세
- `sync_runs`: 동기화 실행 이력
- `sync_errors`: 행별 오류와 원본 식별정보
- 필요 시 `customers`: 납품처 기준정보

이 명칭은 권장안입니다. 구현 전 현재 코드와 실제 ERP 샘플을 확인하고 최소 구조로 확정합니다.

### 2단계 필수 기능

- ERP 어댑터 공통 인터페이스
- CSV 파일 업로드와 컬럼 검증
- 중복 출하를 안전하게 갱신하는 upsert 기준
- 출하 목록과 출하 상세 조회 화면
- 동기화 성공·실패 이력 화면
- 오류 사유 표시와 재시도 기능
- 미등록 품목코드 탐지
- 출하일·출하번호·납품처·차량 기준 검색

### 2단계 시작 전에 받아야 할 자료

- 실제 ERP/PDA 출하 데이터 샘플 10~30행
- 각 컬럼의 업무상 의미
- 출하번호가 전사적으로 고유한지 여부
- 수정·취소 출하의 표시 방법
- PDA 완료 상태값 목록
- 동일 출하번호가 재수신될 때 적용할 갱신 기준
- ERP 연계 가능 방식(API, DB, CSV)과 담당자 확인

자료를 받기 전에는 임의 ERP 규격에 종속되지 않도록 예제 CSV와 컬럼 매핑 화면을 사용합니다.

### 2단계 완료 조건

- 예제 CSV를 업로드하면 출하 헤더와 상세가 DB에 저장됩니다.
- 같은 파일을 다시 올려도 출하가 무한 중복되지 않습니다.
- 잘못된 행은 정상 행과 분리되어 오류 사유를 확인할 수 있습니다.
- 출하번호를 선택하면 품목·수량·납품처·차량·PDA 상태가 보입니다.
- 동기화 실행 이력과 실패 건수를 확인하고 실패 건을 재시도할 수 있습니다.
- 자동 테스트, 린트, 타입 검사, 빌드, 브라우저 확인을 통과합니다.
- 2단계 커밋을 GitHub에 푸시한 뒤 멈춥니다.

## 13. 3단계 이후 상세 방향

### 3단계: 파렛트 환산과 적재 검증

- ERP 출하수량을 `파렛트당 수량`으로 나누어 완전 파렛트와 잔량을 계산
- 잔량 파렛트는 담당자가 보정 가능하게 하고 사유 기록
- 바닥면적, 부피, 중량 기준 적재율 계산
- 최대중량, 적재함 높이, 최대 적층수, 회전금지, 파렛트 겹침 검증
- 적재 불가 물량과 기준정보 미등록 품목 목록
- 동일 출하 건을 5t·5t축·11t에 가상 배치해 비교할 데이터 구조 준비

### 4단계: 자동 배치와 3D 검토

- 처음에는 규칙 기반 3차원 bin-packing 배치 사용
- 최적화 엔진은 인터페이스 뒤에 두어 추후 OR-Tools 등으로 교체 가능하게 설계
- 다중 납품처는 먼저 내릴 화물을 도어 가까이에 두는 하차 역순 상차 규칙 적용
- 3D 화면에서 이동, 90도 회전, 적층, 선택, 위반 강조
- 변경 시 적재율과 위반 상태를 즉시 다시 계산
- 자동안과 수동안을 구분하고 확정 버전·변경 사유 저장
- 확정본 링크와 작업지시용 이미지/PDF 출력

### 5단계: 월별 출하계획 대비 실적

- 월별 계획을 ERP 또는 파일에서 수신
- 전체, 납품처, 품목, 차량 기준 계획·실적 비교
- 달성률: `누적 실적 ÷ 최신 월 계획 × 100`
- 월 경과일 기준 계획선 대비 앞섬·지연 표시
- 일별 누적 계획선과 실적선 차트
- 미달 수량과 월말 예상 달성률
- 최초 계획, 변경 계획, 최신 계획, 실제 실적을 구분하여 보관

### 6단계: 운영 안정화

- 사용자 인증과 역할별 권한
- 확정·승인·예외처리 감사 이력
- 데이터 백업과 복구 절차
- ERP 장애 및 재동기화 운영 절차
- 실제 차량 실측값과 축하중 계산 검증
- 사용자 설명서와 관리자 설명서

## 14. 아직 구현하지 않은 기능

- ERP/API/DB/CSV 실제 연동
- 출하 건과 출하 상세 저장
- 파렛트 수 자동 환산과 잔량 보정
- 적재율 계산
- 축하중 계산 모델
- 자동 배치 알고리즘
- 3D 적재 화면
- 상차계획 확정·버전·승인
- 작업지시서와 PDF
- 월별 계획 대비 실적 분석
- 로그인과 권한
- 기준정보 수정·일괄 업로드
- 운영 서버 배포와 운영 DB 이전

화면에 보이는 후속 기능 안내는 로드맵 표시일 뿐 실제 구현된 기능이 아닐 수 있으므로 API와 테스트를 기준으로 판단합니다.

## 15. 설계 시 특히 주의할 사항

- 차량의 법적 총중량, 실제 허용 적재중량, 축하중은 서로 다른 개념이므로 한 필드로 합치지 않습니다.
- 축하중 계산에는 축 위치, 차체 자체중량, 무게중심 정보가 필요합니다. 현재 `axle_limit_kg`만으로 정밀 계산할 수 있다고 가정하지 않습니다.
- 파렛트 적층 가능 여부와 품목 적층 가능 여부가 다를 수 있으므로 향후 품목별 예외 규칙을 추가할 수 있게 합니다.
- 3D 표시 좌표와 최적화 엔진 좌표의 원점·축 방향·단위를 하나로 정의합니다. 내부 단위는 mm와 kg을 권장합니다.
- 출하번호만 고유하지 않을 수 있으므로 ERP 회사코드·사업장·출하번호 조합 여부를 실제 데이터로 확인합니다.
- PDA 완료시간을 실제 출하실적으로 볼지, ERP 전표 확정시간을 실적으로 볼지 업무 기준을 확정해야 합니다.
- 계획 변경 시 이전 값을 덮어쓰지 말고 버전으로 보관해야 최초 계획 대비 분석이 가능합니다.
- 자동 배치 결과보다 사람이 수정한 확정안을 우선하며 변경 사유와 사용자를 기록해야 합니다.
- 중량 초과, 적층 금지, 하차 순서 위반은 승인·사유 없이는 확정하지 못하도록 설계합니다.

## 16. 비밀정보와 Git 관리

다음 항목은 절대로 코드, Markdown, CSV 예제, 커밋 메시지에 넣지 않습니다.

- ERP 운영 계정과 비밀번호
- DB 접속 문자열
- API 키와 토큰
- 고객사의 실제 개인정보 또는 기밀 출하자료

환경별 접속정보는 `.env` 또는 배포 환경의 비밀변수로 관리합니다. `.env*`, `.wrangler`, `node_modules`, `dist`는 `.gitignore`로 제외되어 있습니다.

작업 시작 시 사용자 변경사항이 있는지 `git status`로 먼저 확인하고, 임의로 삭제하거나 되돌리지 않습니다.

## 17. 다른 PC의 Codex에 전달할 시작 문구

아래 문구를 새 작업의 첫 메시지로 사용할 수 있습니다.

```text
이 저장소의 HANDOFF.md와 README.md를 먼저 전부 읽고 현재 구현과 Git 상태를 확인해줘.
전체 프로젝트 중 2단계인 ERP/PDA 출하정보 연동 기반만 구현해.
실제 ERP 규격이 없으면 CSV 어댑터를 먼저 동작하게 만들고 API/읽기 전용 DB는 교체 가능한 구조로 준비해.
기존 1단계 기능을 유지하고 테스트, 린트, 타입 검사, 프로덕션 빌드, 주요 화면 확인을 모두 해.
완료하면 커밋과 GitHub push까지 한 뒤 반드시 멈추고 3단계는 시작하지 마.
```

실제 ERP 샘플을 준비했다면 위 문구와 함께 샘플 파일 위치와 컬럼 설명을 전달합니다.

## 18. 현재 작업 재개 지점

현재 코드는 1단계가 완료되어 `origin/main`에 올라가 있습니다. 다른 컴퓨터에서는 최신 `main`을 받은 뒤 이 문서를 기준으로 2단계를 시작하면 됩니다.

이번 인수인계 문서 작성 이후의 커밋 해시는 `git log -1 --oneline`으로 확인합니다.
